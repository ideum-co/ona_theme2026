<section class="locations-header section section--page-width" data-section-id="{{ section.id }}">
  <h1>{{ page.title | default: 'Locations' | escape }}</h1>
</section>

{% schema %}
{
  "name": "Locations header",
  "settings": []
}
{% endschema %}
